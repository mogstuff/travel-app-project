import { displayErrors } from '../src/client/js/errorHandler';

describe("testing display errors function", () => {
    test('It should exist as a function', () => {
        expect(displayErrors).toBeDefined();
});

});